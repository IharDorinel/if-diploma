import React from 'react';

const SaleContent = ({props}) => {
console.log(props)
  return (
    <>
      {/*{props.map((elem) => (*/}
      {/*  <div key={elem.id}>*/}
      {/*    {elem.type}*/}
      {/*    {elem.name}*/}
      {/*  </div>*/}
      {/*))}*/}

    </>
  )
};

export default SaleContent;